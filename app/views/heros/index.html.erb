<% content_for :title, "Vos héros| MyLittleHero" %>
<!-- FIRST CONTAINER -->
<div class="hero-index-title-container">
  <div class="hero-title text-center">
    <h2>Tous vos Héros </h2>
    <p> Une nouvelle arrivée dans votre famille?</p>
  </div>
</div>
<div class="flex-button-body">
    <div class="hero-index-tabs text-center">
      <div class="hero-index-tab">
        <%= link_to "Un nouvel héros ?", new_hero_path%>
      </div>
    </div>
  </div>
<!-- SECOND CONTAINER -->
<div class="hero-index-cards-container">
  <div class="row">
    <div class="heros-index">

      <% @heros.each do |hero|%>
        <div class="col-xs-12 col-md-4">
          <div class="hero-index-content text-center">
            <div class="hero-index-block">
              <div class="hero-index-photo">
                <%= link_to hero_adventures_path(hero) do %>
                  <% if hero.photo.blank? %>
                    <%= image_tag("smiling_baby.png", height: 300) %>
                  <% else %>
                    <%= cl_image_tag hero.photo, height: 300, class: 'avatar-index-badge' %>
                  <% end %>
                <% end %>
              </div>
              <h2><%= hero.firstname.capitalize %> <%= hero.lastname.capitalize %></h2>
              <p><%= hero.relatives.where(user_id: current_user.id).first.role %> </p>
              <%= link_to  hero_path(hero), class: "btn hero-show-button" do %>
                  <i class="fa fa-eye"></i> <br> profil
              <% end %>
              <%= link_to new_hero_adventure_path(hero), class: "btn hero-show-button-two" do %>
                  <i class="fa fa-plus"></i> <br> aventures
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

  <!-- ROW ADD NEW HERO -->


<!-- # if @relative.user.count == 1 %>
<div class="row">
  <div class="col-xs-12">

  </div>
</div>
# elsif @relative.user.count == 2 %>
<div class="row">
  <div class="col-xs-12 col-md-6">

  </div>
</div>
# else %>
<div class="row">
  <div class="col-xs-12 col-md-4">

  </div>
</div> -->



